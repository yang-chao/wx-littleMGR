// const base = "https://api.revelations.cn/"
const base = "http://140.143.12.108/"
const arrange = base + "arrange/"
const schedule = base + "schedule/"
const slot = base + "slot/"
const slotUpdate = base + "slot/update"
const studioStatus = base + "studio/"

module.exports = {
  arrange: arrange,
  schedule: schedule,
  slot: slot,
  slotUpdate: slotUpdate,
  studioStatus: studioStatus
}